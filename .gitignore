node_modules
dist
.env
.env.local
.env.*.local
.claude/

scripts/__pycache__/

# Sibling projects with their own .git repos — never track in research-hub
# even if a copy accidentally lands inside the working tree.
life-tracker/
calisthenics-tracker/
