[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# SPA fallback: serve index.html for any path so client-side routing
# and direct deep-links don't 404.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
